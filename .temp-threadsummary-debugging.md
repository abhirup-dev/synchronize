# Thread Summary Debugging Agents

Temporary note for the live synchronize SQL/thread-summary debugging runtime.

Purpose:
- Use this file as a recap anchor after context compaction.
- It records how the throwaway runtime was created, which real agents were launched, where they were placed, and which live browser UI prompts caused them to exercise the MCP SQL/thread tools.
- Do not treat this as a durable project doc; it is intentionally a root-level temporary debugging artifact.

Runtime:
- `SYNCHRONIZE_HOME=/tmp/synchronize-sql-live.ULORGu`
- UI: `http://localhost:55244/web`
- Master commit under test: `8bc308268e2b3e588a640cd7b83ed63fa6b875bb`

Runtime setup summary:
- Stale synchronize daemon/MCP/agent launch processes were cleaned up first.
- `make install-all` was run from `master` so the CLI, MCP adapter, Claude wiring, and Pi wiring used the latest SQL/thread MCP tools.
- A fresh daemon was started against the throwaway `SYNCHRONIZE_HOME`.
- The Codex in-app browser was opened to the runtime UI and used for visible message injection.
- Initial group/thread data was seeded before the real-agent phase, then preserved for follow-up thread-summary work.

Live agents launched:

- `csql-probe`
  - Tool: Claude
  - Group: `#sql-war-room`
  - Thread root tested: `34`
  - Prompt sent from the browser UI as event `74`
  - Reply persisted as event `76`
  - Reported use of `bridge_list_threads`, `bridge_get_thread_status`, `bridge_get_thread`, and `bridge_query_events`

- `csql-audit` / `claude-validation-agent`
  - Tool: Claude
  - Group: `#release-checks`
  - Thread root tested: `43`
  - Prompt sent from the browser UI as event `75`
  - Reply persisted as event `77`
  - The agent appears to have renamed its session to `claude-validation-agent` while retaining the `csql-audit` group alias
  - Reported consistent counts across thread status, thread listing, thread JSON, and SQL query results

- `psql-probe`
  - Tool: Pi
  - Group: `#pi-field-notes`
  - Thread root tested: `58`
  - Prompt sent from the browser UI as event `78`
  - Reply persisted as event `79`
  - Reported use of `bridge_list_threads`, `bridge_get_thread_status`, `bridge_get_thread`, and `bridge_query_events`
  - Confirmed `sender_tool` distinguished `pi`, `claude`, and `web`

Current verified thread status after replies:

- Root `34`: `reply_count=6`, `event_count=7`, `participant_count=6`
- Root `43`: `reply_count=7`, `event_count=8`, `participant_count=6`
- Root `58`: `reply_count=5`, `event_count=6`, `participant_count=6`

Data was intentionally left in place for upcoming thread-summary debugging.
