# Build output — regenerated by `bun run build.ts`.
# dist/        → daemon bundle (/web/ base), served by the daemon
# dist-mobile/ → Capacitor app bundle (/ base), bundled into the APK
dist/
dist-mobile/
