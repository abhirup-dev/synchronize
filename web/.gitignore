# Build output — regenerated by `bun run build.ts`.
dist/

# Storybook static build output — regenerated by `bun run storybook:build`.
storybook-static/

# Vitest coverage output — regenerated by `bun run test:storybook`.
coverage/
