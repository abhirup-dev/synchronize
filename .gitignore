node_modules/
.env/
.demo-synchronize/
.dev-synchronize/
.synchronize-itest/
*.log
.DS_Store

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# bd export artefact at repo root (real state lives in .beads/issues.jsonl)
/issues.jsonl

# graphify knowledge-graph outputs (regeneratable via /graphify)
graphify-out/

# Understand Anything knowledge-graph outputs
.understand-anything/

# Local UI reference captures
docs/ui-reference/claude-design-screenshots/
.codex-artifacts/
mobile/.od-skills/
mobile/od-sketches/

# Claude-created nested worktrees should stay local
.claude/worktrees/

# Local Claude launch profiles are machine-specific
.claude/launch.json

# Local scratch / vendored exploration trees
work/

# Python bytecode from the integration-aoe harness
__pycache__/
*.pyc

# /design-sync
.ds-sync/
ds-bundle/
.design-sync/sb-reference/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Local secrets — NEVER commit (GCP service-account keys for ad-hoc tooling like Gemini vision)
.secrets/
