node_modules/
.demo-synchronize/
.dev-synchronize/
*.log
.DS_Store

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# bd export artefact at repo root (real state lives in .beads/issues.jsonl)
/issues.jsonl

# graphify knowledge-graph outputs (regeneratable via /graphify)
graphify-out/
